[build]
dockerfilePath = "docker/Dockerfile.backend"

[deploy]
startCommand = "uv run fastapi run backend/main.py --host 0.0.0.0 --port 8000"
healthcheckPath = "/hello"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
